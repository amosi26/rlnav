RL Nav Pojects

debug_env, gym_env_test, test_game_window are all test files to ensure each library is working as expected

the gym wrapper is stored in gym_env
the pygame the agent is training on is stored in simple_nav_env 
the ppo is trainined in the file train_ppo_nav

Goal: use RL to create an agent that gets from point a to point b utilizing PPO and later adapt into ros2 and gazebo to control a robot
